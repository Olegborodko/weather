
<%= form_for :form_region, url: {controller:"works", action: "create"}, html: {class: "user_form"} do |f| %>
    <!-- remote: true -->
    <div class="form-group">
      <%= f.text_field :name, class: "form-control", placeholder: "city" %>
    </div>
    <div class="form-group">
      <%= f.submit "select", class: "btn, btn-default" %>
    </div>
<% end %>

<br><br>

<div id="result"></div>